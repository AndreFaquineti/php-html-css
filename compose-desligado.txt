version: '3.8'
services:
  app:
    build: .
    ports:
      - "8080:80"
    volumes:
      - ./src:/var/www/html
    environment:
      MYSQL_HOST: mysql
      MYSQL_USER: root
      MYSQL_PASSWORD: secret
      MYSQL_DB: dbSisUsuarios
    depends_on:
      - mysql

  mysql:
    image: mysql:8.0
    volumes:
      - sistema-usuarios-dados:/var/lib/mysql
    environment:
      MYSQL_ROOT_PASSWORD: secret
      MYSQL_DATABASE: dbSisUsuarios
    command: --default-authentication-plugin=mysql_native_password

volumes:
  sistema-usuarios-dados: